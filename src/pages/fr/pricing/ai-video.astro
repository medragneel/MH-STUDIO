---
import AIVideoPricingLayout from '../../../layouts/AIVideoPricingLayout.astro';
import { pricingTranslations } from '../../../i18n/translations';
import type { Locale } from '../../../i18n/translations';
const locale: Locale = 'fr';
const p = pricingTranslations[locale].aiVideo;
---
<AIVideoPricingLayout locale={locale} seoTitle={p.eyebrow + ' — 26.MH Studio'} data={p} />
